@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">

    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-dark " data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                @*  <img src="~/images/person.jpg" class="float-right" width="5%" />  *@
                <i class="fas fa-user "></i>&nbsp;@User.Identity?.Name!
            </a>
            <div class="dropdown-menu">
                @*                 <a class=" dropdown-item nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" role="button" aria-haspopup="true" title="Manage">@User.Identity?.Name!</a>
            *@                <form class="dropdown-item form-inline text-dark" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link text-dark">
                        <i class="fas fa-sign-out-alt "></i>Logout
                    </button>
                </form>
            </div>
        </li>
    }

    else
    {
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-dark float-md-right" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-user-plus "></i>&nbsp; Register        /Login
            </a>
            <div class="dropdown-menu">
                <a class="dropdown-item nav-link text-black" asp-area="Identity" asp-page="/Account/Register">
                    <i class="fas fa-user-plus "></i>&nbsp; Register
                </a>
                <a class="dropdown-item nav-link text-black" asp-area="Identity" asp-page="/Account/Login">
                    <i class="fas fa-user-plus "></i>&nbsp; Login
                </a>
                @*  <a class="dropdown-item nav-link text-black" asp-area="Identity" asp-page="/Account/LoginWith2fa">
            <i class="fas fa-user-plus "></i>&nbsp; LoginWith2fa
            </a> *@
            </div>
        </li>

        @*  <li class="nav-item">
    <a class="nav-link text-black" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
    <a class="nav-link text-black" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
    <li class="nav-item">
    <a class="nav-link text-black" asp-area="Identity" asp-page="/Account/LoginWith2fa">LoginWith2fa</a>
    </li> *@

    }
</ul>
